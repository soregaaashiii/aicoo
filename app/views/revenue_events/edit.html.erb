<% content_for :title, "収益記録を編集" %>

<% header_actions = capture do %>
  <%= link_to "詳細を見る", @revenue_event, class: "button secondary" %>
  <%= link_to "一覧へ戻る", revenue_events_path, class: "button secondary" %>
<% end %>

<div class="aicoo-page">
  <%= render "shared/aicoo_header",
             title: "収益記録を編集",
             description: "事業評価用の売上・費用イベントを編集します。",
             active_main: :revenue,
             actions: header_actions %>
  <%= render "form", revenue_event: @revenue_event %>
</div>
